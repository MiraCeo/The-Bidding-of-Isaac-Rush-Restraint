# AI 人格模块 v1

本文件记录 AI 人格系统的第一版设计。当前设计的 **21 种人格已全部实现**；正式随机池暂时启用其中 **20 种**，每名人机在对局开始时随机选取 **2～4 个性格**。孤注一掷保留完整实现与专项测试，但在首轮平衡完成前不进入正式随机池。

首批性格主要修正行为权重；中等难度性格进一步接入公开历史、道具价值、道具类型和合作区位置；最后六种人格负责调整分析强度、跨房间资金策略、人格抑制和报价波动。性格本身不划分强弱或稀有度。

## 首批已实现性格

| 性格 | 高价 | 合作 | 扰乱高价 | 扰乱合作 | 观望 |
|---|---:|---:|---:|---:|---:|
| 稳定 | ×1.03 | ×1.08 | ×0.60 | ×0.65 | ×1.00 |
| 扰乱 | ×0.90 | ×0.90 | ×1.50 | ×1.40 | ×0.90 |
| 谨慎 | ×0.70 | ×0.82 | ×0.80 | ×0.90 | ×1.35 |
| 冒进 | ×1.35 | ×1.20 | ×1.10 | ×1.05 | ×0.70 |

跟风与叛逆根据最近三个已公开市场的扰乱率和参与率动态计算：

- 跟风在扰乱率达到 50% 时提高两类扰乱，否则提高高价与合作；高参与市场降低观望，低参与市场提高观望；
- 叛逆采取相反方向：扰乱成为主流时提高正常竞争，正常竞争成为主流时提高扰乱；高参与市场提高观望，低参与市场降低观望。

性格抽取规则：

1. 当前2、3、4个性格等概率；
2. 二十个正式启用性格进入同一随机候选池；孤注一掷可通过显式实验池启用；
3. 稳定与扰乱、谨慎与冒进、跟风与叛逆分别互斥；
4. 多个性格修正累乘，最终性格总乘数限制在 `0.35～2.50`；
5. 性格不能恢复主义中基础权重为 0 的行为；
6. 首批六个性格只影响行为概率；中等性格可以影响目标选择和合作区位置，但仍不直接放大最终报价金额。

当前完整顺序为：主义基础权重 → 资金修正 → 局势分析 → 道具分析 → 性格修正 → `±10%` 决策扰动 → 按最终权重抽取行为 → 动态报价。

## 中等难度已实现性格

### 共赢与慷慨

| 性格 | 高价 | 合作 | 扰乱高价 | 扰乱合作 | 观望 | 合作位置 |
|---|---:|---:|---:|---:|---:|---|
| 共赢 | ×0.90 | ×1.40 | ×0.70 | ×0.82 | ×0.80 | 合作区中心 |
| 慷慨 | ×0.72 | ×1.40 | ×0.62 | ×0.82 | ×0.90 | 合作区下侧约72%半径处 |

慷慨的边缘位置降低其在“距离中心最近的一半玩家”中的排序，但仍保持在合作区内，不直接放弃获取资格。

### 稳健、退让与冲动

设当前两件道具的最高价值为 `Vmax`、平均价值为 `Vavg`：

- 稳健：高价乘数 `0.72 + 0.70Vmax`，合作乘数 `0.82 + 0.42Vavg`，观望乘数 `1.18 - 0.42Vmax`；
- 退让：高价乘数 `1 - 0.62Vmax`，合作乘数 `1 - 0.30Vmax`，观望乘数 `1 + 0.45Vmax`；
- 冲动：高价乘数 `0.90 + 1.15Vmax`，合作乘数 `0.95 + 0.35Vavg`，观望乘数 `1.05 - 0.42Vmax`。

### 投机与守旧

- 投机：把每个非零最终权重变换为 `sqrt(最终权重 × 主义基础权重)`，压平极端概率，同时不恢复主义硬禁用行为；
- 守旧：上一房间所选行为在本房间的权重乘以1.55；首个房间没有历史行为，不触发。

### 虚荣与贪婪

- 虚荣：可见的积分类道具所在组，目标选择权重乘以2.20；并根据该道具价值提高高价与合作、降低观望；
- 贪婪：以相同方式偏好经济类道具；
- 问号道具不会泄露类型，也不会触发类型偏好。

### 当前冲突

- 稳定 ↔ 扰乱；
- 谨慎 ↔ 冒进、冲动；
- 退让 ↔ 冒进、冲动；
- 跟风 ↔ 叛逆；
- 共赢、慷慨 ↔ 扰乱；
- 投机 ↔ 守旧。

## 最后六种已实现人格

### 适应、理智与傲慢

三者不读取当前房间的秘密行动，只复用决策层已经生成的公开局势与道具分析结果，并改变这些结果偏离中性值 `1` 的幅度。

- **适应**：局势分析影响强度乘以 `1.45`，道具分析影响强度乘以 `1.10`；同时略微提高主动得分行为，积分排名越落后，合作竞争与扰乱合作的倾向越高。
- **理智**：局势分析影响强度乘以 `1.35`；资金越低于合理节奏，越压低高价竞争和扰乱高价，转向合作竞争、扰乱合作或少量观望；近期竞争失败率也会进一步压低高价竞争。
- **傲慢**：局势分析影响强度乘以 `0.45`，道具分析影响强度乘以 `0.65`，因此更多保留主义基础权重和自身既有判断。

对原始分析乘数 `M` 应用影响强度 `k` 时，使用 `M^k`，而不是把最终行为权重直接乘以 `k`。这样 `M=1` 始终保持中性，也不会改变修正方向。

### 孤注一掷

只在第一层生效：

- 当本房间最高道具价值低于 `0.55` 时，高价、合作、扰乱高价、扰乱合作分别乘以 `0.45 / 0.55 / 0.55 / 0.65`，观望乘以 `2.20`；
- 道具价值不低于 `0.55` 时，提高主动行为并把观望乘以 `0.55`；
- 一旦抽中主动行为，报价器提交该行为在通用规则下允许的最大合法报价；正常竞拍最多使用全部当前资金，扰乱仍受当前资金 `50%` 上限约束；
- 第二、三层完全取消上述特殊状态。

### 固执与赌徒

- **固执**：其他人格累乘结果以 `0.50` 次幂向中性值收缩；局势和道具分析影响分别乘以 `0.65`、`0.80`；所有修正完成后，再把非零策略权重以 `0.55` 次幂拉回主义基础权重。它不会恢复主义中原本为零的行为。
- **赌徒**：其他人格累乘结果以 `0.72` 次幂向中性值收缩；最终报价随机扰动由通常的 `±5%` 扩大到 `±15%`。合法性裁剪仍在扰动后执行。

### 新增冲突

- 适应 ↔ 傲慢；
- 理智 ↔ 傲慢、孤注一掷、赌徒；
- 孤注一掷 ↔ 谨慎、退让；
- 固执 ↔ 投机；
- 赌徒 ↔ 谨慎。

冲突表按双向关系执行；生成器会跳过与已抽人格冲突的候选，不会通过重抽突破 2～4 个性格上限。

## AI 决策层 v1（历史背景）

本节保留人格模块提出时的四策略决策背景。现行五行为、三种主义及最新权重以 `AI主义模块_v1.md` 和代码实现为准。

AI 的一次行动分为“策略选择”和“报价生成”两个阶段。决策层先计算四种策略的最终权重并抽取其中一种；策略确定后，再进入对应的报价系统。权重只是用于加权随机抽取，总权重不要求等于 100。

### 基础策略权重

| 策略 | 基础权重 | 含义 |
|---|---:|---|
| 高价竞争 | 20 | 以正常竞拍参与 A 或 B，倾向通过较高报价争取道具；当前资金低于本层初始资金的 50% 时，该权重清零。 |
| 合作竞争 | 50 | 以正常竞拍参与 A 或 B，倾向靠近预测的合作区争取合作收益。 |
| 扰乱市场 | 10 | 选择 A 或 B 作为扰乱投放组，并进入扰乱报价系统。 |
| 放弃竞争 | 20 | 本房间静观其变，不报价、不消耗资金。 |

高价竞争与合作竞争不是两种新的基础行动，二者最终都提交正常竞拍；区别在于报价目标和报价强度。

### 权重修正流程

四项基础权重依次受到以下因素修正：

1. **局势分析修正**
   - 当前资金排名；
   - 当前积分排名；
   - 近期已公开的市场状态；
   - 已结算房间中的历史行为。
2. **道具价值修正**
   - 道具强度；
   - 道具类型；
   - 收益周期；
   - 当前已经拥有的道具。
3. **人格模块修正**
   - 按照 AI 当前拥有的性格调整对应策略权重；
   - 人格默认只改变行为出现的相对可能性，不直接指定最终行为；
   - 部分人格还可以在策略选定后修正报价，但不能绕过正常决策与报价流程。
4. **随机扰动**
   - 每项修正后的策略权重分别乘以 `0.9～1.1` 的随机系数；
   - 该扰动表示相对 `±10%`，不是增减 10 个固定权重点。

当前已实现局势分析中的第一项：**资金节奏修正**。设本层初始资金为 `F`，包含当前房间在内的剩余房间数为 `R`，则：

`P = 当前资金 / (F × R / 5)`

`P` 表示当前资金相对于“把本层初始资金均匀留给剩余房间”的充裕程度。按区间对四种策略权重乘以下列系数：

| 资金状态 | 条件 | 高价竞争 | 合作竞争 | 扰乱市场 | 放弃竞争 |
|---|---:|---:|---:|---:|---:|
| 充裕 | `P ≥ 1` | ×1.00 | ×1.00 | ×1.00 | ×1.00 |
| 略紧 | `0.75 ≤ P < 1` | ×0.60 | ×0.90 | ×1.20 | ×1.50 |
| 紧张 | `0.50 ≤ P < 0.75` | ×0.20 | ×0.75 | ×1.50 | ×2.00 |
| 危急 | `P < 0.50` | ×0 | ×0.50 | ×2.00 | ×3.00 |

资金越少，扰乱市场的相对权重越高；同时放弃竞争的增幅更大，用于避免 AI 把“低资金时更倾向扰乱”错误实现为持续耗尽资金。原有的绝对限制仍保留：当前资金低于 `0.50F` 时，高价竞争权重清零。

当前房间的选择与报价是秘密信息。局势分析只能读取游戏规则允许公开的信息以及已经结算的历史信息，不能读取其他玩家在当前房间尚未公开的行动。

### 最终策略抽取

设四种策略经过全部修正和随机扰动后的权重分别为 `wᵢ`，总权重为：

`W = Σwᵢ`

策略 `i` 的最终抽取概率为：

`P(i) = wᵢ / W`

例如最终权重为 `30 / 75 / 5 / 15` 时，总权重为 `125`，对应概率分别为 `24% / 60% / 4% / 12%`。最终权重不需要保持总和为 100。

实现时应保证权重不为负数。如果全部权重意外变为 0，则回退到本节定义的基础权重。除非后续人格规则明确规定某种行为被完全禁止，否则人格修正不应擅自把该行为永久移出选择集合。

### 进入报价系统

策略抽取完成后进入报价系统：

- **高价竞争**：选择 A 或 B，根据道具价值、可用资金和竞争强度生成正常竞拍报价；
- **合作竞争**：选择 A 或 B，根据对共同基准线与合作区的预测生成正常竞拍报价；
- **扰乱市场**：选择扰乱投放组，并在资金上限内生成扰乱金额；
- **放弃竞争**：直接提交静观其变，不生成报价。

第一版临时报价使用本层基础初始资金 `F` 作为计算基准：

| 策略 | 基础报价 |
|---|---:|
| 高价竞争 | `0.60F` |
| 合作竞争 | `0.25F` |
| 扰乱市场 | `0.20F` |

基础报价乘以 `0.8～1.2` 的随机系数，即进行相对 `±20%` 的出价修正，然后四舍五入为整数并执行行动合法性裁剪：

- 正常竞拍不得超过当前资金；
- 扰乱金额不得超过当前资金的 `50%`；
- 修正或裁剪后无法形成正整数报价时，回退为放弃竞争。

报价还受到剩余房间预算约束。设单房预算 `B = 当前资金 / R`：

- 高价竞争上限：`min(0.60F × 随机修正, 3B, 当前资金)`；
- 合作竞争上限：`min(0.25F × 随机修正, 1.25B, 当前资金)`；
- 扰乱市场上限：`min(0.20F × 随机修正, B, 当前资金 × 50%)`。

所有上限在提交前向下取整，最终报价仍必须为正整数。该预算只约束临时报价器，不改变玩家行动的通用合法性规则。

因此，完整临时流程为：

1. 读取 `20 / 50 / 10 / 20` 四项基础策略权重；
2. 根据 `P` 应用资金节奏修正；当前资金低于 `0.50F` 时，另将高价竞争权重清零；
3. 继续应用其他局势分析、道具价值和人格模块修正；
4. 对每项策略权重应用相对 `±10%` 随机扰动；
5. 按 `P(i) = wᵢ / Σwᵢ` 抽取最终策略；
6. 按策略取得 `60% / 25% / 20%` 的基础报价；
7. 对报价应用相对 `±20%` 随机修正并四舍五入；
8. 按单房预算、当前资金和行动规则裁剪后提交。

A、B 目标选择以及更多选择系列的双行动报价分配仍待后续道具估值与 AI 规则确定；在此之前，临时报价器只生成单行动。

## 性格列表

### 扰乱倾向

| 性格 | 功能 |
|---|---|
| 稳定 | 执行扰乱市场的概率下降。 |
| 扰乱 | 执行扰乱市场的概率上升。 |

### 合作倾向

| 性格 | 功能 |
|---|---|
| 共赢 | 以合作共赢为目标，优先争取合作区收益与道具。 |
| 慷慨 | 以合作共赢为目标，优先争取合作区收益；会尽量靠近合作区边缘以让出道具，但不影响最终道具的获取。 |

### 竞争倾向

| 性格 | 功能 |
|---|---|
| 稳健 | 根据道具价值正向调整竞争概率与强度。 |
| 谨慎 | 降低对所有道具的竞争。 |
| 退让 | 极大幅度降低对高价值道具的竞争。 |
| 冒进 | 提高对所有道具的竞争。 |
| 冲动 | 极大幅度提高对高价值道具的竞争。 |

### 状态判断

| 性格 | 功能 |
|---|---|
| 适应 | 根据资金排名、市场状态和历史结果调整策略的权重增加，倾向于增加积分获取。 |
| 理智 | 根据资金排名、市场状态和历史结果调整策略的权重增加，倾向于避免资金浪费。 |
| 傲慢 | 根据资金排名、市场状态和历史结果调整策略的权重降低，自我选择的权重增加。 |

### 趋势响应

| 性格 | 功能 |
|---|---|
| 跟风 | 参考公开趋势并对主流选择进行模仿。 |
| 叛逆 | 参考公开趋势并刻意避开主流选择。 |

### 行为惯性与随机性

| 性格 | 功能 |
|---|---|
| 投机 | 在其他策略之间进行带权随机选择，不影响正常报价。 |
| 守旧 | 自己上一个行为的策略对下一次行动的影响增加。 |

### 特殊资金策略

| 性格 | 功能 |
|---|---|
| 孤注一掷 | 在且仅在第一层，若道具质量较低，则空过房间的概率增加；一旦确定出价，则尽可能消耗当前持有的所有资金。 |

### 道具类型偏好

| 性格 | 功能 |
|---|---|
| 虚荣 | 极大提高对积分类道具的竞争。 |
| 贪婪 | 极大提高对经济类道具的竞争。 |

### 性格抑制与报价波动

| 性格 | 功能 |
|---|---|
| 固执 | 降低其他性格生效的概率，选择初始策略的概率提高。 |
| 赌徒 | 降低其他性格生效的概率，在选定出价后进行更大范围的增减调整。 |

## 人格池边界

- 当前共有 **21 个已实现性格**，正式随机池启用其中 **20 个**。
- 孤注一掷属于“已实现、暂不随机出现”的实验性人格，不代表人格强弱或稀有度分级。
- 当前每名人机从正式人格池随机获得 **2～4 个性格**。
- 性格只修改既有决策流程，不增加新的基础行动。
- 全部性格采用统一模块管理乘数、分析影响、冲突表、叠加上限和随机抽取。
- 当前倍率属于首轮可运行参数，不代表最终平衡；调整应以可复现对照模拟为依据。
