# 《以撒的竞合：冲·慎》基础机制开发大纲

**英文名：** *The Bidding of Isaac: Rush & Restraint*  
**文档用途：** 供后续模型或开发者接手游戏原型。本文只描述游戏大框架与基础机制，暂时不讨论角色、具体道具效果及道具数值平衡。

## 1. 游戏定位

《以撒的竞合：冲·慎》是一款以 **20 人共同市场、秘密报价、资源管理和连续构筑** 为核心的短局多人博弈游戏。

玩家需要在每个房间中判断：

- 是否参与本轮；
- 选择哪一份房间奖励；
- 通过高价竞争还是接近集体基准争取奖励；
- 是否放弃奖励资格，转而扰乱整个市场；
- 是否保存资金，为后续房间保留行动能力。

游戏的四种基本行动可概括为：

> **竞、合、扰、退。**

## 2. 对局结构

- 每局 **20 名玩家**，当前原型暂定为 **1 名真人玩家与 19 名人机玩家**。
- 当前原型分为 **3 层**。
- 当前房间分布为 **5 / 5 / 5**，合计 **15 个房间**。
- 当前各层基础资金规模为 **100 / 500 / 1000**；进入新层时，所有玩家资金重置为该层对应的初始资金，累计积分不重置。
- 玩家在连续房间中消耗和管理资金、获取积分与构筑资源。
- 对局结束后按照累计总积分进行排名。

房间数量、层级资金和完整对局时长应当作为可配置参数实现，当前目标时长约为 10 分钟。

## 3. 单房间结构

每个房间同时提供两份不同奖励，记为 **A** 与 **B**。

每名玩家在该房间选择以下四种行为之一：

| 行为 | 是否消耗资金 | 是否正常争夺奖励 | 是否获取积分 | 是否影响市场 |
|---|---:|---:|---:|---:|
| 竞拍 A | 是 | 是，进入 A 组 | 是 | 是 |
| 竞拍 B | 是 | 是，进入 B 组 | 是 | 是 |
| 扰乱市场（选择 A 或 B 组） | 是 | 否 | 是 | 是，且影响被放大 |
| 不参与 | 否 | 否 | 否 | 否 |

正常竞拍会把玩家分为 A、B 两个局部竞争组，但两个组并不是两场独立游戏。

## 4. 全房间共同市场

- 每个房间只形成 **一条全场共同基准线**。
- 基准线依据房间内 20 名玩家的当轮报价与行动统一计算。
- A、B 两组不分别计算基准线。
- 因此，A 组玩家会影响 B 组结算，B 组玩家也会影响 A 组结算。
- 扰乱者必须选择投放到 A、B 中的一个组；该选择决定其进入哪个合作区，但其市场等价出价仍会影响整个房间的共同基准线。

这条规则是 20 人博弈成立的关键：玩家既要判断自己所选奖励组的竞争，也要预测整个房间的市场行为。

### 共同基准线公式

为避免与奖励 A 组混淆，将本房间全场平均市场出价记为 `M`。设正常竞拍人数为 `n`，扰乱人数为 `m`，正常竞拍实际出价为 `bᵢ`，扰乱实际出价为 `dⱼ`：

`M = (Σbᵢ + Σ2.5dⱼ) / (n + m)`

静观其变的玩家不进入分子，也不进入参与人数。每名正常竞拍者或扰乱者在分母中均只计一人。

本房间共同目标值（基准线）为：

`T = (2 / 3) × M + C`

其中 `C` 暂定为本层初始资金的 10%。按当前三层初始资金 `100 / 500 / 1000` 计算，各层 `C` 分别为 `10 / 50 / 100`。

内部计算保留完整小数精度，不在计算基准线或距离时提前取整；界面显示时可单独格式化。如果本房间所有玩家均静观其变，则不形成共同基准线，本房间也不产生积分或奖励。

该公式的稳定点为本层初始资金的 30%：当 `M` 等于初始资金的 30% 时，`T = M`；低于该比例时常量会向上托起目标值，高于该比例时目标值低于平均市场出价。

## 5. “竞”：最高价路线

- 正常竞拍与投放到 A、B 的扰乱者分别进入对应竞争组，并按市场等价出价共同排序。
- 每组最高价名额数量为该组全部有效市场样本数的 `20%`，四舍五入且至少为 1。
- 正常竞拍者占据最高价名额时获得一份完整奖励；扰乱者占据时名额轮空。
- 该路线强调通过更高资金投入，提高获得目标奖励的确定性。
- 扰乱者参与最高价排序，但没有直接领取奖励的资格；其市场等价出价进入前 `20%` 时会占据并令对应最高价名额轮空。

## 6. “合”：合作区路线

- 基准线附近存在一个 **合作区**。
- 合作区宽度暂定为本层初始资金的 **10%**，按当前三层配置分别为 **10 / 50 / 100**。
- 正常竞拍者与投放到该组的扰乱者均可能进入合作区，并计入合作区人数与距离排名。
- 对每个奖励组分别统计合作区人数，记为 `k`。
- 该组产生的合作奖励名额为：

  `ceil(k / 2)`

- 合作区内玩家按照其计分等价出价与基准线的距离，由近到远排序；距离并列时采用纯随机决胜。
- 全体合作区玩家排名的前 `ceil(k / 2)` 个位置形成合作奖励名额。名额由正常竞拍者占据时，该玩家获得一份完整奖励；由扰乱者占据时，该名额直接轮空。

示例：

| 合作区人数 k | 合作奖励名额 |
|---:|---:|
| 0 | 0 |
| 1～2 | 1 |
| 3～4 | 2 |
| 5～6 | 3 |
| 7～8 | 4 |
| 9～10 | 5 |

合作区人数越多，能够产生的奖励越多；但玩家仍需争取进入距离基准线最近的一半。人数为奇数时，合作名额向上取整。

### 名额重合与轮空

- 同一玩家同时获得最高价资格与合作区资格时，只获得一份奖励。
- 该重复名额不向后顺延。
- 扰乱者可以增加合作区人数、参与距离排序并占据合作名额，但其取得的任何奖励名额均直接轮空，不向后顺延。

## 7. “扰”：扰乱市场

扰乱是独立于正常竞拍的主动行为。玩家支付资金、选择投放到 A 或 B 组，并放弃本房间的正常奖励资格，以换取更强的市场影响力和正常积分资格。

设扰乱者提交的实际出价为 `d`：

- `d` 正常从玩家当前资金中扣除；
- `d` 不得超过玩家行动前当前资金的 **50%**；
- 其用于影响全场市场和基准线的 **市场等价出价** 为 `2.5d`；
- 其用于个人积分计算、合作区判定和距离排序的 **计分等价出价** 为 `1.25d`；
- 扰乱者选择的 A 或 B 组决定其进入哪个合作区，但不限制其对全场共同基准线的影响；
- 扰乱者可以正常获取本轮积分；
- 扰乱者不能直接通过最高价或合作区获得奖励；
- 扰乱者占据的最高价或合作奖励名额直接轮空；持有超凡升天时，每个被占据名额分别有 `50%` 概率转化为实际道具资格。

50%资金上限只限制单次扰乱规模，不限制扰乱次数。连续扰乱会因为剩余资金减少而自然降低后续可投入上限。

> **实现约束：** 扰乱支出是主动行为成本，支付后不予退还。

## 8. “退”：不参与房间

玩家可以主动放弃整个房间：

- 不提交有效竞拍；
- 不消耗资金；
- 不获得本轮积分；
- 不取得任何奖励资格；
- 不主动影响本轮市场。

不参与不是异常状态，而是正式策略。玩家可以牺牲当前房间的收益，保存资金与后续行动能力。

## 9. 积分、资金与胜负

- 正常竞拍者按照其报价与全场基准线的距离获得本轮基础积分。设玩家计分出价为 `x`，共同基准线为 `T`，则：

  `score = 100 × e^(-|x - T| / σ)`

- `σ` 暂定为本层初始资金的 10%。按当前三层资金 `100 / 500 / 1000` 计算，三层 `σ` 分别为 `10 / 50 / 100`。
- 正常竞拍者以实际出价作为 `x`；扰乱者使用 `1.25d` 作为计分等价出价 `x`，然后代入同一公式。
- 当计分出价与共同基准线完全相等时获得单轮最高基础分 `100`。偏差为 `1σ / 2σ / 3σ` 时，基础分约为 `36.79 / 13.53 / 4.98`。
- 房间最终积分倍率为：普通房与商店房 `×1`，宝箱房 `×1.5`，隐藏房与 BOSS 房 `×2`。该倍率作用于当前房间完整得分，并与道具积分乘算项累乘。
- 积分曲线在共同基准线两侧对称。积分结果存储时保留四位小数，界面显示时保留两位；基准线等规则计算值也按四位小数保存，计算过程不提前取整。
- 不参与者本轮不获取积分。
- 正常竞拍出价在提交后正常扣除；无论是否取得最高价或合作区奖励，均不返还竞拍支出。
- 玩家需要在当前房间收益与后续资金之间作出跨房间决策。
- 全部房间结束后，以累计总积分决定最终排名。

### 人机人格模块（v1）

每名人机在对局开始时随机选取 **2～5 个性格**，由多个性格共同修改其决策。AI 使用高价竞争、合作竞争、扰乱市场、放弃竞争四项基础策略权重，经局势、道具价值、人格和随机扰动修正后加权抽取策略，再进入报价系统。第一版共记录 21 个性格，完整定义参见 [AI人格模块_v1.md](./AI人格模块_v1.md)。人格只修改既有决策流程，不新增基础行动；冲突表、权重、叠加顺序和具体参数将在后续版本确定。

> **待确认：** 首批道具规则已经确定，但具体数值平衡与 AI 人格参数仍需通过后续实现和模拟继续调整。最高价并列和合作区距离并列目前均采用纯随机决胜；实现使用可注入随机源，以便回放时复现随机结果。

## 10. 核心循环

每个房间的核心循环为：

1. 展示 A、B 两份房间奖励；
2. 玩家查看当前资金与局势；
3. 在竞拍 A、竞拍 B、扰乱市场、不参与之间作出选择；选择扰乱时，还需指定投放到 A 或 B 组；
4. 所有选择与报价同时秘密提交；
5. 根据全房间市场形成基准线；
6. 结算正常积分与扰乱积分；
7. 分别结算 A、B 两组的最高价资格与合作区资格；
8. 处理重合和轮空名额；
9. 更新积分、资金与玩家状态；
10. 进入下一房间，最终按总积分排名。

## 11. 设计核心

四种行为分别对应四种明确意图：

| 核心行为 | 玩家意图 |
|---|---|
| 竞 | 以资金换取奖励确定性 |
| 合 | 通过预测全场共识争取奖励 |
| 扰 | 放弃奖励资格，付费改变整个市场 |
| 退 | 放弃当前收益，保存后续资源 |

一句话概括：

> **20 名玩家在单房双奖励的共同市场中，通过竞争、合作、扰乱或退出管理资金和风险，以连续房间中的积分积累争夺最终排名。**

## 12. 后续开发边界

接手模型或开发者应优先完成并验证以上基础循环，不应在基础框架尚未稳定前继续增加新的基础行动。

开发时应确保：

- 四种行动在界面中清晰区分；
- 全场基准线与两个局部奖励组的关系可视化；
- 合作区人数、名额数量、距离排名及轮空原因可以被玩家理解；
- 扰乱的实际出价、2.5 倍市场影响、1.25 倍计分出价分别展示；
- 结算结果能够解释玩家为什么获得或没有获得积分与奖励；
- 所有倍率、层级资金、房间数量和区域宽度均参数化，避免写死。

道具类别、稀有度和房间生成概率参见 [道具与道具池_v1.md](./道具与道具池_v1.md)；首批 26 件道具及统一结算顺序参见 [首批道具与结算流程_v1.md](./首批道具与结算流程_v1.md)。后续继续扩充道具池，并通过模拟调整道具联动与数值平衡。
