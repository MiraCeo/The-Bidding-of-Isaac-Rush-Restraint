# 报价决策系统 v1

> 本文保留首版实现。当前优化规则见《报价决策系统 v2》；v2 将模型输出统一定义为道具处理前的目标等价出价。

本版本将报价从 AI 行为决策中拆出。行为决策仍负责选择“高价竞争、合作竞争、扰乱市场或放弃”，报价系统只负责在行为已经确定后生成名义报价。

## 一、流程

```text
基础定价
→ 资金比例修正
→ 市场状态修正
→ 道具价值修正
→ 最高市场等价出价预测 / 合作区预测
→ 人格模块修正（暂时留空）
→ 最终随机扰动（±5%）
→ 节奏上限与行动合法性检查
→ 整数最终报价
```

所有阶段的中间值均保留在 `AiQuoteAnalysis` 中，供模拟报告和后续调参使用。

## 二、基础定价

以本层初始资金 `F` 为基准：

| 行为 | 基础定价 |
|---|---:|
| 高价竞争 | `60%F` |
| 合作竞争 | `25%F` |
| 扰乱市场 | `20%F` |

基础定价只是报价计算的起点，不再直接作为最终报价。

## 三、资金比例修正

继续使用决策端的资金节奏指标：

```text
P = 当前资金 / (本层初始资金 × 剩余房间数 / 5)
```

报价倍率暂定为：

```text
高价竞争：clamp(0.35 + 0.72P, 0.35, 1.20)
合作竞争：clamp(0.50 + 0.50P, 0.50, 1.10)
扰乱市场：clamp(0.50 + 0.38P, 0.50, 1.00)
```

资金降低时三类报价都会收缩，高价竞争收缩最快。扰乱仍通过计分等价出价 `×1.25` 以较低名义资金接近合作区。

## 四、市场状态修正

读取最近三个房间的：

- 共同基准线与本层初始资金之比；
- 市场参与率；
- 扰乱行为占比；
- 最高市场等价出价与本层初始资金之比。

市场越热，高价、合作和扰乱报价均可小幅上升；扰乱已经较多时，扰乱报价会适当下降。市场修正有明确上下限，不会单独支配报价。

## 五、道具价值修正

复用决策层形成的目标组估值，估值已经包含：

- 道具强度；
- 道具类型；
- 收益周期；
- 已拥有道具强度；
- 重复道具折价；
- 道具间组合；
- 决策与道具组合。

高价竞争对目标道具价值最敏感；合作竞争保持较温和的正向调整；没有超凡升天时，扰乱对高价值道具略微降价，持有超凡升天后改为随道具价值提高报价。

短程硫磺火、幸运脚、巴比伦大淫妇、硫磺火、神圣屏障和 Steam 大促具有对应的报价组合修正。报价修正小于决策概率修正，避免同一组合被重复放大。

问号道具继续只使用统一期望值，不读取实际隐藏内容。

## 六、个人市场预测

每名 AI 根据相同的公开历史独立形成预测，不使用全体 AI 共用的最终预测值：

```text
预测最高市场等价出价 = 近期最高市场等价出价均值 × 私有误差
预测共同基准线 = 近期共同基准线均值 × 私有误差
预测合作区 = 预测共同基准线 ± 本层初始资金的10%
```

没有历史样本时，最高市场等价出价默认预测为 `60%F`，共同基准线默认预测为 `30%F`。

当前私有预测误差暂定：

- 最高市场等价出价：`±10%`；
- 共同基准线：`±6%`。

高价竞争将自身修正报价与预测最高市场等价出价混合。合作竞争围绕预测合作区报价；扰乱市场先用 `1.25` 计分倍率将预测合作区换算回名义资金，再生成报价。

预测误差和最终报价扰动使用不同的随机抽取，因此不会同步变化。

## 七、人格与随机扰动

人格倍率当前固定为 `1`，只保留接口。

所有有效修正和预测完成后，最终报价只进行 `±5%` 随机扰动：

```text
随机倍率 = Random(0.95, 1.05)
```

该随机范围刻意保持较低，不能盖过资金、市场和道具价值判断。

## 八、最终边界

- 报价资金只保留整数，使用四舍五入；
- 普通竞拍不得超过当前资金；
- 扰乱市场不得超过当前资金的 50%；
- 继续应用剩余房间节奏预算；
- 计算结果不大于 0 时转为放弃；
- 名义报价、实际扣款、市场等价出价和计分等价出价继续分开处理。

本版本仍只生成单行动；“更多选择”系列的双行动报价将在后续单独接入。
