# 首批道具与结算流程 v1

本文件正式记录首批 26 件道具、相关术语、积分结构以及道具资格与发放顺序。未标注“多个道具叠加无效果”的道具，在获得多个副本时正常叠加；具体平衡数值仍可在模拟后调整。

## 一、道具清单

### BOSS·稀有（6件）

#### 妈刀

新增一条额外得分加成。计算方式与基础得分相同，但目标值改为当前房间所有市场样本中最高市场等价出价的 `1/4`，基础倍率改为 `50`，`σ` 保持不变。该额外得分优先于其他比例加算道具。正常竞拍、完美视力修正后的竞拍以及扰乱市场均以各自的市场等价出价参与最大值比较，双行动的两份报价分别参与。

#### 圣心

后续得分乘以 `150%`。

#### 硫磺火

参与正常竞拍时，计算得分可在实际出价的 `80%～125%` 区间内选择能够取得最高分的计分值。只影响积分计算，不影响共同基准线和道具争夺。

#### 妈妈的心脏

BOSS 房中的得分乘以 `200%`，包括取得该道具的当前房间。获得后追溯修正当前 BOSS 房得分。第三层 BOSS 房固定生成两个妈妈的心脏，不执行常规道具抽取。

#### 幸运脚

通过正常竞拍进入合作区时，无论合作区距离排名如何，均可获得一份对应道具。

#### 神圣屏障

每层获得一层临时屏障。上一层未消耗的临时屏障不继承；取得该道具时立即获得一层当前层可用的临时屏障。若同时存在临时屏障和持久屏障，优先消耗临时屏障。

### BOSS·普通（4件）

#### 短程硫磺火

参与扰乱市场时，计算得分可在计分等价出价的 `85%～120%` 区间内选择能够取得最高分的计分值。只影响积分计算，不影响共同基准线和道具争夺。

#### 五芒星

后续得分乘以 `125%`。

#### 嗝屁猫的项圈

每层资金首次低于本层初始资金的 `25%` 时，有 `50%` 概率获得本层初始资金的 `25%`。取得该道具后立即进行一次当前资金是否低于 `25%` 的检查。

#### 超凡升天

通过扰乱市场占据最高价或合作道具名额时，每个名额分别有 `50%` 概率实际获得对应道具资格。若同一玩家在同一组同时赎回两个名额，重复资格仍按同组资格合并规则处理。多个超凡升天叠加无效果。

### 常见·稀有（5件）

#### 完美视力

争夺道具时，竞拍出价按 `1.25` 倍计入出价之和及道具竞争。获取积分时仍使用原始出价，不使用放大后的数值。

#### 殉道者之血

每次获得分数时，得分加 `20`。该固定加分进入积分加算区，优先于比例乘算效果。

#### Steam大促

后续正常竞拍的实际扣款减少 `30%`。名义报价不得高于当前资金，该道具不提高可提交的名义报价上限，只减少实际扣款；不作用于扰乱市场。

#### 偷钱袋

每进入一个新房间，窃取现金排行榜中位于自己前一名玩家当前资金的 `5%`，并从对方账户扣除。若自己排名第一，则窃取后一名玩家。多个偷钱袋按照各自取得时间依次结算。

#### 穷人护符

当前资金低于本层初始资金的 `30%` 时，每次得分加 `20`；否则每次得分加 `5`。固定加分进入积分加算区，优先于比例乘算效果。该道具不再扣除资金。

### 常见·普通（5件）

#### 福袋

返还取得该道具时本次竞拍实际扣款的 `75%`。若嗝屁猫的尾巴使玩家获得两份福袋，则共返还本次实际扣款的 `150%`。

#### 积分护符

每次获得分数时，得分加 `10`。该固定加分进入积分加算区，优先于比例乘算效果。

#### 利息

每个房间开始时，当前资金增加 `5%`；下一层初始资金增加 `10%`。

#### 六面骰

每个房间有 `1/6` 概率使持有者当前房间得分乘以 `200%`。

#### 木质十字架

获得一层持久屏障；未消耗的持久屏障可以跨层保留。若同时存在临时屏障和持久屏障，优先消耗临时屏障。

### 诅咒·稀有（3件）

#### 更多选择

可以同时对房间的 A、B 两个道具进行正常竞拍，并分别获得两份竞拍计分；选择双竞拍后不能再选择扰乱市场。两份报价视为两个独立市场样本，名义报价总和不得超过当前资金的 `50%`。使用双竞拍时，当前房间得分乘以 `75%`；雅阁和以扫无视该积分乘区。

若同时取得 A、B 两个道具的获得权，随机摧毁其中一个。多个更多选择叠加无效果。

#### 更多选择?

可以同时对 A、B 两组进行扰乱市场，并分别获得两份扰乱计分；选择双扰乱后不能再选择正常竞拍。两份扰乱视为两个独立市场样本，每份扰乱金额分别不得超过行动前当前资金的 `50%`。使用双扰乱时，当前房间得分乘以 `75%`；雅阁和以扫无视该积分乘区。

该道具可以与超凡升天搭配；若双扰乱分别取得两个道具获得权，则可以同时保留两件道具。多个更多选择?叠加无效果。

#### 黑王子之冠

整局结算时，总得分加成初始为 `+40%`。每拥有一个除黑王子之冠外的其他道具，总得分加成减少 `5%`，最低不低于 `-10%`。

黑王子之冠可以叠加；多个黑王子之冠不会被计为导致该加成下降的“其他道具”，每个王冠分别提供自己的总分倍率。

### 诅咒·普通（3件）

#### 永恒六面骰

后续得分乘以 `150%`，但每个房间有 `25%` 概率使持有者当前房间得分清零。

#### 巴比伦大淫妇

后续得分乘以 `125%`；若持有者的实际竞拍出价低于共同基准线，则当前房间得分清零。

#### 嗝屁猫的尾巴

获得道具时：有 `1/3` 概率使获得的道具消失，有 `1/3` 概率正常获得一份，有 `1/3` 概率获得两份。该效果不作用于嗝屁猫的尾巴自身，多个嗝屁猫的尾巴叠加无效果。

对于没有注明“多个道具叠加无效果”的道具，尾巴产生的两个副本均正常生效。

## 二、屏障效果

屏障分为临时屏障和持久屏障：

| 类型 | 来源 | 跨层规则 |
|---|---|---|
| 临时屏障 | 神圣屏障 | 当前层有效，未消耗层数不继承；每层重新获得 |
| 持久屏障 | 木质十字架 | 未消耗时跨层保留，直至触发 |

正常竞拍未取得对应道具获得权时，退回该次竞拍的全部实际扣款，随后扣除一层屏障。若两种屏障同时存在，优先扣除临时屏障。

与更多选择搭配时：

- 一个报价成功、一个失败：退回失败报价的实际扣款；
- 两个报价都失败：视为一次屏障触发，退回两笔报价的全部实际扣款；
- 两个报价都成功，随后更多选择随机摧毁一个：不退款，因为两个报价都取得过道具获得权；
- 嗝屁猫的尾巴使已取得获得权的道具消失：不退款。

## 三、积分结构

设玩家在当前房间提交的有效计分行动集合为 `J`。单行动时集合中有一项，使用更多选择或更多选择?时可以有两项。

房间得分为：

`roomScore = Σⱼ(baseScoreⱼ + additiveScoreⱼ) × roomMultiplier × ∏ multiplierₖ`

其中：

- `baseScore` 是对应行动按基础积分公式得到的分数；
- `additiveScore` 包括马刀额外评分曲线、积分护符、殉道者之血、穷人护符等积分加算项；
- `roomMultiplier` 为房间积分倍率：普通房与商店房为 `1`，宝箱房为 `1.5`，隐藏房与 BOSS 房为 `2`；
- `multiplier` 包括圣心、五芒星、妈妈的心脏、六面骰、永恒六面骰、巴比伦大淫妇以及双行动的 `75%` 等积分乘算项；
- 所有积分乘算项累乘；
- 永恒六面骰和巴比伦大淫妇等清零效果在房间积分结算中具有最终清零效果；
- 黑王子之冠在整局累计分数结算阶段应用。

## 四、完整结算流程

每个房间按照以下顺序结算：

1. 确定行动合法性；
2. 修正报价上限；
3. 修正实际扣款；
4. 修正市场等价出价；
5. 计算共同基准线；
6. 修正计分等价出价；
7. 计算基础分和额外评分曲线；
8. 按积分加算区和积分乘算区计算房间得分；
9. 结算最高价与正常合作名额；
10. 幸运脚补充正常竞拍者的道具资格；
11. 超凡升天判定扰乱者占据的合作名额；
12. 合并同一组内的重复资格；
13. 检查玩家是否同时取得 A、B 双资格；
14. 根据上述步骤产生的道具获得权结算屏障退款；
15. 更多选择随机摧毁一项，更多选择?保留两项；
16. 嗝屁猫的尾巴处理剩余道具的消失、正常或翻倍；
17. 正式发放道具；
18. 处理福袋、嗝屁猫的项圈、妈妈的心脏等即时或追溯效果；
19. 处理房间转换时的经济效果；
20. 对局结束时应用黑王子之冠等整局总分倍率。

## 五、当前边界

- 当前首批道具共 26 件；
- 第三层 BOSS 房固定生成两个妈妈的心脏，是常规道具生成流程的例外；
- A、B 两个位置不允许生成同一道具，第三层 BOSS 房固定生成两个妈妈的心脏是唯一例外；不同房间允许重复抽取道具；
- 同一子池内的具体道具暂时等概率抽取，同名道具暂不设置获取上限；
- 资金始终保存为非负整数，每次独立资金变化后立即四舍五入，`.5` 向上取整；
- 房间转换经济顺序为资金重置、利息、偷钱袋；多个百分比经济效果分别乘算并逐次取整；
- 马刀的正式名称修正为妈刀；
- 具体数值平衡仍待后续模拟确定；
- 所有概率判定应使用可注入随机源，以支持种子复盘和批量模拟。
