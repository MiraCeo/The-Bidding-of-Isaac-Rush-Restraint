# 报价决策系统 v2

## 一、核心口径

报价模型首先计算“目标等价出价”，随后才根据行动和已有道具换算名义报价与预计实际扣款：

```text
目标等价出价
→ 行动类型换算
→ 道具效果换算
→ 名义报价
→ 预计实际扣款
→ 合法性与安全上限
```

- 普通合作竞拍以计分等价出价为目标；
- 高价竞争以市场竞争等价出价为目标；
- 扰乱市场按 `1.25` 计分等价倍率反推名义报价；
- 完美视力按竞争倍率反推高价竞争的名义报价；
- Steam 大促不降低名义报价，只降低预计实际扣款；
- 实际结算仍由统一的道具与行动结算模块完成，AI 只使用相同规则进行预估。

## 二、处理顺序

```text
基础定价
→ 资金比例修正
→ 市场状态修正
→ 道具价值修正
→ 个人市场预测修正
→ 人格修正（暂留空）
→ 候选报价效用选择
→ 最终随机扰动 ±5%
→ 行动与道具换算
→ 机会预算安全上限
→ 最终整数报价
```

基础定价继续为高价 `60%F`、合作 `25%F`、扰乱 `20%F`，其中 `F` 为本层初始资金。

## 三、预测不能覆盖资金判断

预测只对资金、市场和道具修正后的结果进行有限调整：

| 资金状态 | 预测最高上调 |
|---|---:|
| 充裕 | +25% |
| 中等 | +15% |
| 较低 | +5% |
| 危急 | 0% |

向下修正最低为预测前报价的 75%。因此低资金 AI 即使预测市场价格很高，也不会被绝对预测值重新拉回高报价。

## 四、跨房间机会预算

AI 根据当前房间和本层剩余房间的期望价值分配资金。当前基础权重为：

| 房间 | 权重 |
|---|---:|
| 普通房 | 1.0 |
| 隐藏房 | 2.0 |
| 宝箱房 | 1.5 |
| 商店房 | 1.6 |
| BOSS 房 | 2.0 |

普通房位置按 `25%` 概率转化为隐藏房计算，其未来期望权重为 `1.25`。当前房间权重还会根据已选道具的个人价值调整。机会预算同时进入候选报价的成本惩罚，并形成一个较宽松的安全上限。它是软成本信号，不会把 AI 硬编码为固定储蓄行为。

房间积分倍率也同步进入 AI 判断：宝箱房的主动策略权重和候选报价积分效用按 `1.5` 倍估值，隐藏房与 BOSS 房按 `2` 倍估值；观望权重不放大。双行动的预计积分增量同样应用当前房间倍率。商店房没有额外积分倍率，其 `1.6` 机会权重仅反映更高价值的道具池。

## 五、个人预测与置信度

每个 AI 开局生成并在整局保留：

- 最高市场等价出价的长期高估或低估偏差；
- 共同基准线的长期高估或低估偏差；
- 在合作区中偏向左侧、中心或右侧的位置偏差。

每个房间仍产生小幅独立预测误差。预测置信度由样本数量和近期市场波动共同决定：样本更多且市场稳定时更依赖历史，样本不足或市场剧烈波动时向默认先验收缩。

合作报价不会全部瞄准同一个中心点，而是在个人预测合作区内选择不同位置。预测随机流、报价随机流和策略决策随机流彼此分离。

## 六、候选报价

AI 围绕模型报价生成三档候选：

```text
低报价：模型值 × 70%
标准报价：模型值 × 100%
高报价：模型值 × 125%
```

每档候选估算：

- 与预测共同基准线的积分距离；
- 对目标道具的预计竞争能力；
- 超凡升天等组合带来的扰乱获取价值；
- 道具个人估值；
- 预计实际扣款相对于当前机会预算的成本；
- 当前资金稀缺程度。

候选以效用形成权重后随机抽取，仍然允许次优和激进行为，不采用确定性最优报价。

## 七、随机与边界

- 最终随机扰动严格限制为 `±5%`；
- 资金只保留整数，最终报价四舍五入；
- 普通竞拍名义报价不超过当前资金；
- 扰乱市场不超过当前资金的 50%；
- 机会预算只提供软成本和安全上限；
- 人格倍率当前固定为 `1`；
- 持有“更多选择”或“更多选择?”时，可在第一份行动后进入条件式第二行动判断。

## 八、双行动条件决策

第一份行动仍由正常策略与报价系统生成。若玩家持有对应道具，则对另一组分别生成高价、合作与停止追加候选。

决策剩余资金按第一份**名义报价**保守扣除，不提前把 Steam 大促减少的实际扣款重新视作可用资金。在正式行动规则之外，AI 的双行动还使用四层资源限制：

- 至少保留 `max(当前资金 ÷ 本层剩余房间数，本层初始资金 × 10%)` 作为下一房间储备；最后一间房不要求该储备；
- 两份行动的名义报价总和不超过行动前资金的 `40%`；
- 两份行动的预计实际总扣款不超过 `min(行动前资金 × 40%，本层初始资金 × 17%)`；
- 停止追加的基础权重由 `1.15` 提高为 `3.0`，追加仍然是概率选择，而不是满足条件后必然执行。

上述限制只约束 AI 是否追加第二行动，不改变真人玩家的行动合法性。双竞拍和双扰乱仍同时遵守道具自身的正式上限。

设第一份行动单独预计得分为 `S1`，加入第二份行动后的两份预计得分为 `S1'`、`S2`：

```text
第二行动预计积分增量 = 0.75 × (S1' + S2) - S1
```

雅阁和以扫无视该 `0.75` 积分乘区，其第二行动预计积分增量改为 `S1' + S2 - S1`。这一角色被动同时作用于 AI 预测和正式房间结算。

预测时会把两份报价共同加入私人市场估计，重新计算共同基准线，因此 `S1'` 不一定等于 `S1`。第二行动总效用由积分增量、另一件道具价值、资格概率、道具组合、扰乱破坏价值、资金机会成本和后续房间储备共同组成，再与停止追加一起按权重随机抽取。

“更多选择”候选为第二次高价竞拍、第二次合作竞拍、停止追加；“更多选择?”候选为第二次扰乱高价、第二次扰乱合作、停止追加。停止追加只取消第二份行动，第一份行动仍正常提交。隐藏道具只使用未知道具期望价值。

## 九、模拟观测

模拟器新增：报价倍率分布、裁剪率、平均裁剪幅度、共同基准线预测误差、最高等价出价预测误差、合作区命中率、单位实际资金得分、各房间实际投入比例、各价值区间报价比例，以及商店/BOSS 房无资金率。
