# 游戏端流程 v1

本文件记录第一版可游玩客户端的范围与页面状态。当前目标是尽快把已经实现的房间、角色、AI、报价、道具和结算模块串成一局完整游戏，不以华丽的开始界面、存档或联网功能为前置条件。

## 第一版范围

- 电脑端网页游戏；移动端适配暂不进入本阶段。
- 打开游戏后直接进入角色选择，不制作独立首页、登录页或复杂开场动画。
- 房间行动沿用当前游戏界面和交互布局。
- 当前房间的其他 AI 行动在玩家提交前保持未知。
- 玩家提交后才统一生成 AI 决策，并立即执行该房间结算，不设置二次结算确认。
- 结算详情与榜单页分开；结算后可以查看积分排行或争夺榜，也可以跳过榜单直接进入下一房间。
- 第三层最终房间结算后显示整局最终排行，并允许重新开始。
- 第一版不实现存档、对局回放、离线下载包和服务器部署。

## 页面状态机

```text
角色选择
  ↓ 确认角色
房间行动
  ↓ 玩家提交
生成 AI 行动并立即结算
  ↓
结算详情
  ├─ 积分排行 ↔ 争夺榜
  ├─ 从任一榜单进入下一房间
  └─ 直接跳过榜单
  ├─ 尚有房间 → 玩家确认 → 下一房间行动
  └─ 最终房间 → 最终排行 → 重新开始
```

建议在代码中把状态显式定义为：

1. `character_selection`：玩家选择角色，系统创建玩家、19 名 AI 和本局随机状态。
2. `room_action`：显示当前房间、两件道具、资金排行和市场；只接受玩家行动。
3. `room_result`：玩家行动锁定后生成 AI 行动、立即调用正式结算流程，并展示本房间结算详情。
4. `room_ranking`：顶栏“排行”对应的独立榜单页，可切换20人当前积分排行与A/B道具争夺榜。
5. `game_result`：第三层结束后展示整局最终积分排名。

状态只能按上述方向转换。玩家提交后不能返回修改本房间行动；进入下一房间前不提前生成下一轮 AI 决策。

## 角色选择

角色选择页第一版只需要：

- 五个角色卡片：以撒、游魂、阿撒泻勒、伊甸、雅阁和以扫；
- 显示角色初始道具和资金特性；
- 当前选择状态；
- 一个“开始游戏”按钮。

点击开始后一次性创建本局状态：玩家角色、AI 角色、AI 主义、2～4 个正式池人格、第一层资金和第一房间。正式人格池采用当前 20 种人格，不随机抽取孤注一掷。

## 房间行动

继续使用当前界面：

- 顶栏保留游戏、排行、设置、规则入口；
- 左侧资金排行和右侧共同市场可以长期展开，不遮挡中心操作；
- 中央显示 A、B 道具；
- 每件道具下方按上下关系显示出价竞拍与扰乱市场；
- 使用滑条、数字输入和 `+1/-1` 同步调整金额；
- 未选行动变灰；
- 玩家可选择出价竞拍、扰乱市场或静观其变；
- “更多选择”系列可按现有规则进入双行动流程。

玩家点击确认后：

1. 锁定玩家本房间行动；
2. 禁用金额、目标与行动方式的修改；
3. 使用本局保存的 AI 状态生成 19 名 AI 的行动；
4. 合并本房间全部行动并立即调用正式结算流程；
5. 进入结算详情页，不再要求玩家二次确认结算。

AI 不在玩家思考期间预先提交，也不能读取玩家尚未提交的行动。玩家提交是本房间 AI 决策和结算的唯一触发点。

## 简单结算界面

第一版结算不制作复杂动画，至少展示：

- 本房间共同基准线和合作区；
- 玩家提交的行动、名义报价、实际扣款和市场等价出价；
- 玩家本房间获得的基础积分、加算、乘算与最终房间积分；
- A、B 道具资格由谁占据，是否因扰乱轮空、超凡升天、更多选择、屏障或嗝屁猫的尾巴发生变化；
- 玩家是否获得道具以及获得份数；
- 本房间发生的返款、资金恢复、窃取和追溯效果；
- 前往积分排行、争夺榜以及直接进入下一房间的按钮。

积分排行显示每名 AI 的行动类型、目标组、报价、房间得分、获得道具和结算后总分。争夺榜按A、B道具分栏，统计争夺人数并按市场等价出价降序排列；最终获得道具与由扰乱行动占据的名额使用不同颜色标记。两张榜都可以直接进入下一房间。

## 房间推进与终局

- 玩家可在结算详情后查看任一榜单再继续，也可直接跳过榜单进入下一房间。
- 进入房间时依次执行既定的资金重置、利息、穷人护符和偷钱袋流程。
- 房间类型、隐藏房判定、道具类别、稀有度和具体道具均由现有房间生成器负责。
- 每层五个房间；第三层 BOSS 房完成后不再生成新房间。
- 最终排行按最终积分从高到低排列，并显示角色、主义/人格（AI）、道具数量和剩余资金。
- 并列继续采用当前随机规则。

## 实现顺序

1. [x] 建立客户端整局状态和显式页面状态机。
2. [x] 实现角色选择与新游戏初始化。
3. [x] 将当前房间 UI 接入真实玩家状态与行动合法性检查。
4. [x] 在玩家提交后调用 AI 决策，并生成统一的房间结算输入。
5. [x] 接入正式结算流程，生成可供 UI 使用的结构化结算记录。
6. [x] 实现简单结算页、积分排行与“进入下一房间”。
7. [x] 实现层间转换、最终排行和重新开始。
8. [x] 为完整三层流程增加确定随机种子的自动化测试。

## 暂缓内容

以下内容不进入第一版客户端实现，统一记录于 [TODO.md](./TODO.md)：

- 本地存档、恢复对局和回放；
- 可下载的离线游戏包；
- 云服务器 Docker、静态文件托管和发布流程；
- 移动端布局与触控适配。
